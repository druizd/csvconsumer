# csvconsumer/.env.example — Plantilla para nuevos deployments
# Copiar como .env y completar con los valores reales del entorno
RABBITMQ_USER=shipper
RABBITMQ_PASS=
RABBITMQ_URL=amqps://shipper:<PASSWORD>@localhost:5671/
DATABASE_URL=postgres://postgres:<PASSWORD>@localhost:5432/logmetrics?sslmode=require
PORT=8080
